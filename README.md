# Quadratic Root Finder — Newton-Raphson Method

A Python Tkinter application that numerically approximates the **real roots of a quadratic equation** using the **Newton-Raphson method**.

> 🎯 Designed for developers — clean code, robust error handling, and modular structure.

---

## 🔧 Features

- Input coefficients `a`, `b`, `c` for `ax² + bx + c = 0`
- Applies Newton-Raphson with multiple initial guesses
- Detects convergence and handles edge cases (e.g., zero derivative)
- Shows discriminant and residual error at each root
- Responsive GUI with clear output
- Input validation & user-friendly error messages

---

## 🚀 How to Run

1. Clone the repo:
   ```bash
   git clone https://github.com/prorammer2025/quadratic-newton.git
   cd quadratic-newton
